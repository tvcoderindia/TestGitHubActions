name: Build-Workflow

permissions:
  id-token: write
  contents: write

on:
  push:

jobs:
  build:
    runs-on: [windows-latest]
    steps:
      - name: Chekout Code
        uses: actions/checkout@v4
        with:
              fetch-depth: 0
